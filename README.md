# cordova-platform-tests

### Usage:

```
npm install && npm link
cordova-platform-tests [<path/to/platform/repo>] [--verbose]
```

### Notes

This is a very draft implementation that supports android only
